pub mod models;
#[macro_use]
extern crate juniper_codegen;
#[tokio::main]
async fn main() {
    // println!("GraphQL server running on 4050...");
    // warp::serve(crate::routes::routes())
    //     .run(([0, 0, 0, 0], 4050))
    //     .await;
}
